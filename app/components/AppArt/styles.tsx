import {StyleSheet} from 'react-native';

const useStyle = () => {
  return StyleSheet.create({
    imageStyle: {
      width: '100%',
      height: undefined,
      aspectRatio: 1,
    },
  });
};

export default useStyle;
