import {StyleSheet} from 'react-native';

const useStyle = () => {
  return StyleSheet.create({
    imageStyle: {
      height: '100%',
      width: undefined,
      aspectRatio: 1,
    },
  });
};

export default useStyle;
